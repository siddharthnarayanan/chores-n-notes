# chores and small scripts

To run:

1. ```python amwins_api.py``` -- this brings up the flask app server
2. ```requests.post("http://localhost:5000/trigger_fr_models")``` -- post request to the server to trigger pipeline
